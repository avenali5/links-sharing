import styled from "styled-components";

export const FinalResultStyle = styled.div`
  .outer-wrapper {
    box-shadow: 0 5px 30px rgba(0, 0, 0, 0.15);
  }
  .buttons {
    display: none !important;
  }
`;
